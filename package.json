{
  "name": "wdiv-s3-trigger",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:dev": "sls deploy --stage dev",
    "deploy:prod": "sls deploy --stage prod"
  },
  "author": "chris48s",
  "devDependencies": {
    "serverless": "^3.25.1",
    "serverless-python-requirements": "^5.0.0"
  }
}
